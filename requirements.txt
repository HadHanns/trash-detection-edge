# ============================================================
# Trash Detection Edge Computing — Requirements
# Python >= 3.9
# ============================================================

# Core deep learning
ultralytics>=8.3.0          # YOLOv11 (yolo11n.pt) + YOLOv8 fallback
torch>=2.0.0                # PyTorch CPU-only compatible
torchvision>=0.15.0         # NMS utilities (torchvision.ops.nms)

# SAHI — Slicing Aided Hyper Inference
sahi>=0.11.15

# Dataset & image processing
Pillow>=10.0.0
opencv-python>=4.8.0
numpy>=1.24.0
pycocotools>=2.0.7          # COCO API for annotation parsing

# Data management
requests>=2.31.0            # For downloading TACO dataset
tqdm>=4.66.0               # Progress bars
gdown>=4.7.0                # Google Drive download (backup weights)

# Scientific computing
scipy>=1.11.0               # IoU computations, metrics

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
pandas>=2.0.0               # Metrics CSV

# System monitoring (edge simulation)
psutil>=5.9.0               # CPU/RAM monitoring

# Jupyter
jupyter>=1.0.0
ipykernel>=6.25.0
ipywidgets>=8.1.0

# YAML config
PyYAML>=6.0

# Export & optimization
onnx>=1.14.0                # ONNX model export
onnxruntime>=1.16.0         # ONNX inference on CPU

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
